export default function Footer() {
  return (
   <footer className="max-w-5xl mx-auto px-4 sm:px-6 py-8 border-t border-[var(--color-line)] flex items-center justify-between font-[var(--font-mono)] text-xs text-[var(--color-muted)]">
    <span>// end_of_file</span>
    <span>© {new Date().getFullYear()} ralvihan</span>
    </footer>
  );
}